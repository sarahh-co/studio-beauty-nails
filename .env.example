# Cal.com API v2 secret key (Settings > Developer > API keys on cal.com).
# Server-side only — never expose this to the browser. Copy this file to
# .env.local (git-ignored) and fill in the real value there.
CAL_API_KEY=
